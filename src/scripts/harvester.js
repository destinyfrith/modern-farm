// Define and export a harvestPlants function
export const harvestPlants = (usingPlants) => {
    // return an array of seed objects
    let seedObj = []
    for (const plant of usingPlants) {
        // on each plant, get the value of the output property
        
    }
}
